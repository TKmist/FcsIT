1. In the FCS fitting module, B values are not updating (reading from workspace.dct) when shifting between the files.
2. In the FCS fitting module, <B> values are not updating after removing the results from the database. Required to re-fit all to remove the bug.
